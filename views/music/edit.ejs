<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>음원데이터 편집</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <style>
        body,
        html {
            background-color: #f5f5f5;
            width: 100%;
            height: 100%;

        }

        .content {
            position: realtive;
        }

        .content__center {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 500px;
        }

        button {
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="content text-center">
        <div class="content__center">
            <h3 class="font-weight-bold">음원데이터 편집</h3>
            <form action="/update?_method=PUT" method="POST" enctype="multipart/form-data">
                <input type="hidden" name="id" value="<%=locals.data.id%>">
                <input type="hidden" name="existingFilePath" value="<%=locals.data.filePath%>">
                <input class="form-control mt-4" type="text" name="title" placeholder="곡제목" value="<%=locals.data.track%>" required/>
                <input class="form-control mt-2" type="text" name="album" placeholder="앨범명" value="<%=locals.data.album%>" required/>
                <input class="form-control mt-2" type="text" name="artist" placeholder="아티스트" value="<%=locals.data.artist%>" required/>
                <div class="mt-2">기존 파일 : <%=locals.data.filePath%></div>
                <input class="form-control mt-2" type="file" name="music_file"/>
                <div class="mt-3 text-right">
                    <button type="button" class="btn btn-danger col-3" onclick="history.back()">취소</button>
                    <button class="btn btn-primary col-3">편집</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>