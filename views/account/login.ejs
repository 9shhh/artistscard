<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>로그인</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <style>
        body,
        html {
            background-color: #f5f5f5;
            width: 100%;
            height: 100%;

        }

        .content {
            position: realtive;
        }

        .content__center {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 300px;
        }
        
        button {
            width: 100%;
        }
        .warningMsg {
            color: #FF0000;
        }
    </style>
</head>

<body>
    <div class="content text-center">
        <div class="content__center">
            <h3 class="font-weight-bold">로그인</h3>
            <p class="warningMsg"><%=locals.msg%></p>
            <form action="/signIn" method="POST">
                <input class="form-control mt-4" type="text" id="id" name="id" placeholder="아이디" required value="<%=locals.maintainId%>"/>
                <input class="form-control mt-2" type="password" id="password" name="password" placeholder="패스워드" required/>
                <button class="btn btn-primary mt-3">로그인</button>
                <p class="mt-3"><a href="/register">회원가입</a></p>
            </form>
        </div>
    </div>
</body>
</html>